# TODO（未纳入当前阶段）

以下为本轮评审建议中，暂不纳入第一阶段实现基线的事项，作为后续迭代待办：

1. 接口限流基线落地（`@nestjs/throttler`）
   - 登录按 IP + email 限流
   - 发帖/回帖按 user_id 限流

2. 本地一键启动编排（`docker-compose.yml`）
   - postgres / backend / frontend 统一编排

3. 搜索能力路由预留与实现（`GET /api/search?q=&type=`）
   - 第一阶段可先 SQL LIKE，后续升级全文检索

4. 通知中心数据模型落地
   - `notifications(id, user_id, type, source_type, source_id, is_read, created_at)`

---

## 第二阶段暂不实现（后续迭代）

1. 通知与提醒能力
   - @用户提醒
   - 消息通知中心（`@我` / `回复我` / 系统通知）
   - 通知查询与已读管理接口

2. 搜索能力
   - `GET /api/search?q=&type=`（吧/帖子/用户）
   - 检索能力实现与升级路径（LIKE / 全文检索）

3. 平台工程与安全基线增强
   - 接口限流基线（登录按 IP+email，发帖/回帖按 user_id）
   - 本地一键启动编排完善（postgres / backend / frontend）

4. 编辑历史记录
   - 帖子/回复编辑版本存储与展示

5. 帖子/回复二次编辑
   - 帖子作者编辑已发布的帖子（标题和内容）
   - 回复作者编辑已发布的回复内容
   - 已在第二阶段实现计划 §8 中明确为本阶段不实现

6. 用户封禁
   - 吧级用户封禁（版主/吧主操作）
   - 全站用户封禁（管理员操作）
   - 已在第二阶段实现计划 §8 中明确为本阶段不实现

## 后续候选能力（头脑风暴）

> 以下为第二阶段候选但未纳入实现计划的能力，作为后续迭代参考。
> 部分条目已纳入第二阶段实现（管理操作审计、楼主标识等），此处仅保留尚未纳入的条目。

1. 内容治理
   - 帖子/回复举报（用户侧举报入口 + 吧务/管理员处理流程）
   - 删除原因记录与展示
   - 注：管理操作审计已纳入第二阶段实现（见 PHASE2_IMPLEMENTATION_PLAN.md §2.2.3）

2. 发布体验
   - 草稿箱（自动保存草稿、手动保存、草稿列表管理）
   - 自动保存（编辑中定时保存，防丢失）

3. 社区运营
   - 吧内置顶（帖子置顶与取消）
   - 精华标记（帖子标精与取消）
   - 公告能力（吧公告发布与展示）
   - 热门排序（基于互动量的帖子排序算法）

4. 账号与安全
   - 设备会话管理（查看/踢出登录设备）
   - 异常登录提醒
   - 基础风控黑名单

5. 互动增强
   - 回复折叠（低质量或被踩回复自动折叠）
   - 快捷引用（一键引用回复内容）

6. 媒体体验
   - 图片多图上传（批量选择与上传）
   - 图片压缩与封面（自动缩略图生成）
   - 上传失败重传

7. 数据能力
   - 个人中心统计（发帖/回复/获赞数据面板）
   - 吧内活跃榜

8. 工程质量
   - 关键链路埋点
   - 慢查询监控
   - E2E 覆盖补齐
